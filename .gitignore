# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dev-dist
*.local

# Environment variables (contains personal data)
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# GEMINI Code specific files
GEMINI.md

# Claude Code specific files
CLAUDE.md

# Code Review files
docs/code-reviews/

# Local temporary files (screenshots, etc.)
tmp/

# Font reference files (generated by download script)
public/fonts/*.css

# Build-generated data files (only commit on releases)
# These files are auto-generated by scripts/build-data.ts during builds
# and change on every build (timestamps). They should only be committed
# during release commits to avoid unnecessary noise in git history.
# To include them in a release: git add -f <file>
public/data/index.transformed.json
src/data/generated-fallback.ts
